name: Hello

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to user
        run: echo "Hello Ali"
  
      - name: say hello username
        run: echo "Hello $USER"

      
