name: multi-stage

on: 
    workflow_dispatch:

jobs:
    checkout:
        runs-on: ubuntu-latest
        steps: 
            - name: code checkout
              uses: actions/checkout@v4
    

    show-current-file: 
        runs-on: ubuntu-latest
        steps: 
            - name: show current files
              run: ls -a
            