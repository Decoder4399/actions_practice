name: repo_checkout

on: 
    workflow_dispatch:

jobs:
    repo_check:
        runs-on: ubuntu-latest
        steps:
            - name: github checkout
              uses: actions/checkout@v4

            - name: list all
              run: ls -a

