name: 2jobs

on: 
    workflow_dispatch

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: "this is job to prnt Hello"
              run: echo "Hello"

    job2: 
        runs-on: ubuntu-latest
        needs: job1
        steps:
            - name: "this is job to prnt world"
              run: echo "World"